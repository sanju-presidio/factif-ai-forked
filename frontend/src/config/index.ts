// WARNING - Don't enable this in a hosted server environment. This is meant for local frontend and backend deployments only.
export const config = {
  explorer: {
    enabled: true,
    defaultCollapsed: true
  },
  system: {
    homePath: '/Users/'
  }
};
